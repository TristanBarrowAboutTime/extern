import * as React from 'react';
// import { useLoadableSpacexData, RocketUiData } from '../../../network/hooks/useLoadableSpacexData';
import { QueryResult } from '../../../network/Queries';
import BaseLoader from '../loaders/BaseLoader'

const DeleteAbleDetails = () => {
    // const binding = useLoadableSpacexData();
    // const { data, isSuccess } = binding;
    // console.log("rendering", data);

    return (
        <>
        
        </>
    );
}

export default DeleteAbleDetails;